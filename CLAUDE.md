# CLAUDE.md — ehon-yomikikase-demo

## 絶対ルール（全会話・全エージェント適用）

### 削除コマンド禁止
- `rm`, `rm -rf`, `rmdir`, `unlink`, `git clean -df`, `find -delete`, `rsync --delete` 等、一切の削除コマンドを生成・提案しない。
- 削除が必要な場合は「手動で削除してください」と説明するのみ。
- `DELETE/` をゴミ箱として使用する（ファイルを移動する）。

### コード生成禁止事項
- 絵文字禁止（コード・コメント・ドキュメント全て）
- `alert()` / `confirm()` / `prompt()` 禁止（プロジェクト全体）
- グローバル変数禁止（セキュリティ上の理由）
- 制御構文・条件構文以外はクラスまたは関数に書く
- 文字列リテラルは設定ファイルまたはDBに分離（ハードコード禁止）
- フォールバック禁止。例外処理を必ず書く
- コメントはWHYが非自明な場合のみ

---

## 開発フロー

### TDD（厳守）
```
plan → red test → coding → green test
```
- バックエンド: RSpec
- フロントエンド: Jest + Playwright (E2E)
- フロント確認: `curl`, `wget --mirror`, Playwright

### ブランチ戦略
- **mainブランチでの直接作業禁止**
- `src/*` の変更は必ずPRを作成する
- `src/*` 以外（ドキュメント・設定）はmainへ直接pushを許可

### commit前
- `/security-review` を必ず実行する
- QC10.md / OWASP10.md / CC.md / TM.md / DC.md / development-principles.md を参照する

---

## アーキテクチャ

### 基本スタック
| 役割 | 技術 |
|------|------|
| フロントエンド | Next.js |
| バックエンド | Ruby on Rails (API mode) |
| データベース | PostgreSQL |
| AI・解析・画像加工 | FastAPI |
| 高速並列・リアルタイム | Gin (Go) |

### アーキテクチャ方針
- 規模に応じて: マイクロサービス / MVC / API Gateway / メッセージング
- 安全なライブラリ・OSS・SaaSを適用し、車輪の再発明を避ける
- オリジナルコードを最小に保つ

### デプロイ先
| 役割 | サービス |
|------|---------|
| フロントエンド | Vercel（無料プラン） |
| バックエンド・管理画面 | Render または Railway（無料プラン） |

### ドメイン
- `rictaworks.jp` のサブドメインを原則とする

### 認証
- Googleログイン（OAuth 2.0）
- 開発環境では認証済み状態に分岐（テスト可能にする）

---

## 多言語対応

対応言語: **日本語・英語・フランス語・中国語・ロシア語・スペイン語・アラビア語**

- 開発当初から多言語対応を実装する
- 管理画面は日本語のみ

---

## セキュリティ

参照: `.claude/OWASP10.md`

- OWASP Top 10 (2021) 全項目を実装時に確認する
- 環境変数は `.env` を参照する（ハードコード厳禁）
- 入力バリデーションはシステム境界（ユーザー入力・外部API）で実施

---

## 品質管理

参照: `.claude/QC10.md`

- QC01〜QC10 全項目をリリース前にチェックする
- デバッグトレースできるようコードを書く

---

## UIデザイン

- アイコン: **Font Awesome** を使用（絵文字禁止）
- デザイン原則: `.claude/CRAP.md` を参照（Contrast / Repetition / Alignment / Proximity）
- 画像はAI生成を使用する
- プロのライティングは **writerエージェント** が行う

---

## ディレクトリ管理

| ディレクトリ | 用途 |
|------------|------|
| `TASKS/` | タスク管理 |
| `DEBUG/` | バグ報告 |
| `CLIENT/` | クライアント要望 |
| `WORK/` | 作業報告 |
| `ENV/DEVELOPMENT.md` | 開発環境定義 |
| `ENV/PRODUCTION.md` | 本番環境定義 |
| `SPEC/` | 仕様書・図解（ER図・DFD・シーケンス図・クラス図・状態遷移図・ユースケース図） |
| `DELETE/` | ゴミ箱（削除対象ファイルの移動先） |

図解フォーマット: **Mermaid**（`mermaid` をインストールして使用）

---

## エージェント構成

規模に応じて以下のエージェントを活用する。各エージェントの詳細定義は `.claude/agents/` 参照。

| エージェント | 役割 |
|------------|------|
| director | 全体統括・意思決定 |
| project-manager | タスク管理・進捗管理 |
| designer | UI/UX設計・CRAP原則適用 |
| debugger | デバッグ・根本原因分析 |
| tester | テスト設計・実行 |
| data-scientist | データ分析・AI機能 |
| deployer | デプロイ・インフラ管理 |
| writer | プロのライティング・多言語対応 |
| service-manager | 外部サービス管理 |

### サブエージェント

| エージェント | 役割 |
|------------|------|
| pr-checker | 全PRを日本語化・非エンジニア向けユーザーテストをPR本文に記載 |
| tester | PRのユーザーテスト実行スクリプトを `test/pr***/` に作成 |

---

## PR作成ルール

- タイトル・本文は日本語で書く
- **非エンジニア向けユーザーテスト手順**をPR本文に丁寧に記載する
- テスト手順は誰でも実行できる粒度で書く

---

## コンプライアンス

参照: `.claude/CC.md`

CC01〜CC10 を実装・リリース前に確認する。

---

## 開発原則

参照: `.claude/development-principles.md`

YAGNI / KISS / DRY / SOLID を厳守する。
